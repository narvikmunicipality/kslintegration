describe('PersonService', () => {
    const PersonService = require('../../src/services/PersonService');
    var service;

    beforeEach(() => {
        service = new PersonService(undefined);
    });
});